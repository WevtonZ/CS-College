#include <stdio.h>

int main(void)
{
    int mat[10][10];
    int li, co;
    int i, j;

    scanf("%d %d", &li, &co);
    if(li<=0 || co<=0)
    {
        printf("Dimensao invalida\n");
        return 0;
    }

    return 0;
}